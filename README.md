Como subir o banco: docker compose up -d

Como criar schema: rodar scripts em database/scripts

Como rodar API: dotnet run --project src/GregCustomers.Api

Como rodar MVC: dotnet run --project src/GregCustomers.WebMvc

Usuários de teste (admin/reader)

Rotas principais

Decisão logo no banco (e alternativa de URL em produção)